<Window x:Class="QuantFlow.UI.WPF.Windows.Dialogs.AlgorithmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding DialogTitle}" 
        Height="800" 
        Width="1000"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="{DynamicResource BackgroundColor}">

    <Border BorderBrush="{DynamicResource BorderColor}" 
            BorderThickness="1"
            CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="70"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" 
                    Background="{DynamicResource PrimaryColor}"
                    CornerRadius="8,8,0,0"
                    BorderBrush="{DynamicResource BorderColor}"
                    BorderThickness="0,0,0,1">
                <Grid Margin="24,0">
                    <TextBlock Text="{Binding DialogTitle}" 
                               FontSize="20" 
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimaryColor}"
                               VerticalAlignment="Center"/>

                    <Button Content="✕" 
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Width="32" 
                            Height="32"
                            Background="Transparent"
                            BorderThickness="0"
                            FontSize="18"
                            Foreground="{DynamicResource TextSecondaryColor}"
                            Cursor="Hand"
                            Click="CloseButton_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="4">
                                                <ContentPresenter HorizontalAlignment="Center" 
                                                                VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource HoverColor}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- Content -->
            <ScrollViewer Grid.Row="1" 
                          VerticalScrollBarVisibility="Auto"
                          Padding="24,20">
                <StackPanel>

                    <!-- Validation Message -->
                    <TextBlock Text="{Binding ValidationMessage}" 
                               Foreground="#E74C3C"
                               FontSize="12"
                               Margin="0,0,0,10"
                               Visibility="{Binding HasValidationError, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    
                    <!-- Name and Status -->
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Algorithm Name -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Algorithm Name *" 
                                       FontSize="14" 
                                       FontWeight="Medium"
                                       Foreground="{DynamicResource TextPrimaryColor}"
                                       Margin="0,0,0,8"/>
                            <TextBox Text="{Binding AlgorithmName, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource ModernTextBox}"
                                     Height="40"/>
                        </StackPanel>

                        <!-- Status -->
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Status" 
                                        FontSize="14" 
                                        FontWeight="Medium"
                                        Foreground="{DynamicResource TextPrimaryColor}"
                                        Margin="0,0,0,8"/>
                            <ComboBox ItemsSource="{Binding AvailableStatuses}"
                                      SelectedItem="{Binding SelectedStatus}"
                                      Style="{StaticResource ModernComboBox}"
                                      Height="40"/>
                        </StackPanel>
                    </Grid>

                    <!-- Description -->
                    <TextBlock Text="Description" 
                               FontSize="14" 
                               FontWeight="Medium"
                               Foreground="{DynamicResource TextPrimaryColor}"
                               Margin="0,0,0,8"/>
                    <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource ModernTextBox}"
                             Height="60"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             Margin="0,0,0,20"/>

                    <!-- Version and Language Row -->
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Version -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Version" 
                                       FontSize="14" 
                                       FontWeight="Medium"
                                       Foreground="{DynamicResource TextPrimaryColor}"
                                       Margin="0,0,0,8"/>
                            <TextBox Text="{Binding Version, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource ModernTextBox}"
                                     Height="40"/>
                        </StackPanel>

                        <!-- Language -->
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Language" 
                                       FontSize="14" 
                                       FontWeight="Medium"
                                       Foreground="{DynamicResource TextPrimaryColor}"
                                       Margin="0,0,0,8"/>
                            <ComboBox ItemsSource="{Binding AvailableLanguages}"
                                      SelectedItem="{Binding SelectedProgrammingLanguage}"
                                      Style="{StaticResource ModernComboBox}"
                                      Height="40"/>
                        </StackPanel>
                    </Grid>



                    <!-- Algorithm Code -->
                    <TextBlock Text="Algorithm Code *" 
                               FontSize="14" 
                               FontWeight="Medium"
                               Foreground="{DynamicResource TextPrimaryColor}"
                               Margin="0,0,0,8"/>
                    <!-- Algorithm Code -->
                    <TextBox Text="{Binding AlgorithmCode, UpdateSourceTrigger=PropertyChanged}"
                             Height="290"
                             TextWrapping="NoWrap"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             FontFamily="Consolas"
                             FontSize="12"
                             Margin="0,0,0,10"
                             Padding="8"
                             Background="{DynamicResource SurfaceColor}"
                             Foreground="{DynamicResource TextPrimaryColor}"
                             BorderBrush="{DynamicResource BorderColor}"
                             BorderThickness="1">
                        <TextBox.Resources>
                            <Style TargetType="{x:Type TextBox}">
                                <Setter Property="VerticalContentAlignment" Value="Top"/>
                            </Style>
                        </TextBox.Resources>
                    </TextBox>
                    
                </StackPanel>
            </ScrollViewer>

            <!-- Footer -->
            <Border Grid.Row="2" 
                    Background="{DynamicResource SurfaceColor}"
                    BorderBrush="{DynamicResource BorderColor}"
                    BorderThickness="0,1,0,0"
                    CornerRadius="0,0,8,8">
                <Grid Margin="24,0">
                    <StackPanel Orientation="Horizontal" 
                                HorizontalAlignment="Right">
                        <Button Content="Cancel"
                                Style="{StaticResource SecondaryButton}"
                                Width="100"
                                Height="40"
                                Margin="0,0,12,0"
                                Click="CloseButton_Click"/>

                        <Button Content="{Binding SaveButtonText}"
                                Style="{StaticResource PrimaryButton}"
                                Width="100"
                                Height="40"
                                Command="{Binding SaveCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>