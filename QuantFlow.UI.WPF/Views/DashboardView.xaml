<UserControl x:Class="QuantFlow.UI.WPF.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:controls="clr-namespace:QuantFlow.UI.WPF.Controls"
             Background="{DynamicResource BackgroundColor}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="40">
            <!-- Page Title -->
            <TextBlock Text="Dashboard" 
                       FontSize="32" 
                       FontWeight="SemiBold" 
                       Foreground="{DynamicResource TextPrimaryColor}"
                       FontFamily="Segoe UI"
                       Margin="0,0,0,32"/>

            <!-- Section: Market Overview -->
            <TextBlock Text="Market Overview" 
                       FontSize="18" 
                       FontWeight="SemiBold" 
                       Foreground="{DynamicResource TextPrimaryColor}"
                       FontFamily="Segoe UI"
                       Margin="0,0,0,16"/>

            <!-- Market Overview Content -->
            <Grid Margin="0,0,0,40">
                <!-- Loading Spinner -->
                <Border Background="{DynamicResource SurfaceColor}"
                        BorderBrush="{DynamicResource BorderColor}"
                        BorderThickness="1"
                        CornerRadius="12"
                        Height="200"
                        Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <controls:LoadingSpinner SpinnerSize="45" Margin="0,0,0,16"/>
                        <TextBlock Text="Loading market data..." 
                                   FontSize="14"
                                   Foreground="{DynamicResource TextSecondaryColor}"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Empty State Message -->
                <Border Background="{DynamicResource SurfaceColor}"
                        BorderBrush="{DynamicResource BorderColor}"
                        BorderThickness="1"
                        CornerRadius="12"
                        Padding="40"
                        MinHeight="200"
                        Visibility="{Binding HasNoSymbols, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="📊" 
                                   FontSize="48" 
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,16"/>
                        <TextBlock Text="No symbols configured" 
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextPrimaryColor}"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,8"/>
                        <TextBlock Text="Go to Settings to add symbols to track" 
                                   FontSize="14"
                                   Foreground="{DynamicResource TextSecondaryColor}"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Crypto Price Cards -->
                <ItemsControl ItemsSource="{Binding CryptoCards}"
                              ItemTemplate="{StaticResource CryptoCardTemplate}"
                              Visibility="{Binding HasNoSymbols, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </Grid>

            <!-- Section: My Portfolios -->
            <TextBlock Text="My Portfolios" 
                       FontSize="18" 
                       FontWeight="SemiBold" 
                       Foreground="{DynamicResource TextPrimaryColor}"
                       FontFamily="Segoe UI"
                       Margin="0,0,0,16"/>

            <!-- Portfolio Cards -->
            <ItemsControl ItemsSource="{Binding Portfolios}"
                          ItemTemplate="{StaticResource PortfolioCardTemplate}"
                          Margin="0,0,0,40">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>

            <!-- Section: Recent Trades -->
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Recent Trades" 
                           FontSize="18" 
                           FontWeight="SemiBold" 
                           Foreground="{DynamicResource TextPrimaryColor}"
                           FontFamily="Segoe UI"
                           VerticalAlignment="Center"/>

                <Button Grid.Column="1"
                        Content="View All"
                        Style="{StaticResource SecondaryButton}"
                        Width="100"
                        Height="36"/>
            </Grid>

            <!-- Recent Trades List -->
            <Border Background="{DynamicResource SurfaceColor}"
                    BorderBrush="{DynamicResource BorderColor}"
                    BorderThickness="1"
                    CornerRadius="12"
                    ClipToBounds="True">
                <ItemsControl x:Name="RecentTradesControl"
                              ItemsSource="{Binding RecentTrades}"
                              AlternationCount="{Binding RecentTrades.Count}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="Transparent">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
                                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                        <Style.Triggers>
                                            <DataTrigger Value="True">
                                                <DataTrigger.Binding>
                                                    <MultiBinding Converter="{StaticResource IsLastItemConverter}">
                                                        <Binding Path="(ItemsControl.AlternationIndex)" 
                                                                 RelativeSource="{RelativeSource TemplatedParent}"/>
                                                        <Binding Path="Items.Count" 
                                                                 RelativeSource="{RelativeSource AncestorType=ItemsControl}"/>
                                                    </MultiBinding>
                                                </DataTrigger.Binding>
                                                <Setter Property="BorderThickness" Value="0"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <ContentControl Content="{Binding}" 
                                                ContentTemplate="{StaticResource RecentTradeTemplate}"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>